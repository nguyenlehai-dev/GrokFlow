"""Servers module services — paramiko SSH + metrics scraping.

Layout:
    ssh.py     — connection helpers; run_command, run_sudo
    metrics.py — parse system stats (uptime / cpu / mem / disk / network)
"""
