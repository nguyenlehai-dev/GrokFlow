# ─── Domains & TLS ─────────────────────────────────────────────
DOMAIN=grokflow.example.com
API_DOMAIN=api.grokflow.example.com
CADDY_EMAIL=admin@example.com

# Frontend bundle bakes this in at build time. Use the API_DOMAIN
# (or main DOMAIN if you proxy /api through it) accessible from browsers.
PUBLIC_API_URL=https://api.grokflow.example.com

# ─── Postgres ──────────────────────────────────────────────────
POSTGRES_USER=grokflow
POSTGRES_PASSWORD=CHANGE_ME_strong_random_password
POSTGRES_DB=grokflow

# ─── App secrets (regenerate per environment) ──────────────────
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=CHANGE_ME_long_random_hex_string
JWT_ALGORITHM=HS256
JWT_EXPIRES_MINUTES=1440

# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=CHANGE_ME_fernet_key

API_KEY_PREFIX=uxpm_live

# ─── App config ────────────────────────────────────────────────
APP_ENV=production
APP_DEBUG=false

STORAGE_DRIVER=local
MAX_CONCURRENT_JOBS_PER_USER=2
MAX_CONCURRENT_JOBS_PER_PROFILE=1
DEFAULT_DAILY_LIMIT=1000
JOB_TIMEOUT_SECONDS=600

# Allowed origins for the dashboard. Match DOMAIN.
CORS_ORIGINS=https://grokflow.example.com

# ─── Flow API (video-processing-service side-car) ──────────────
# Bootstrap creates admin + API key on first boot; key lands on a shared
# volume so the backend picks it up automatically. See docs/FLOW-SETUP.md.
FLOW_SECRET_KEY=CHANGE_ME_random_hex_for_flow_jwt
FLOW_BOOTSTRAP_EMAIL=flow-admin@grokflow.local
FLOW_BOOTSTRAP_USERNAME=flowadmin
FLOW_BOOTSTRAP_PASSWORD=CHANGE_ME_strong_random_password
FLOW_MAX_UPLOAD_MB=500

# Optional Cloudflare R2 — leave empty for on-disk mode.
FLOW_R2_ACCOUNT_ID=
FLOW_R2_ACCESS_KEY_ID=
FLOW_R2_SECRET_ACCESS_KEY=
FLOW_R2_BUCKET_NAME=video-output
FLOW_R2_PUBLIC_URL=

# ─── Image tag (set by CI on deploy) ───────────────────────────
TAG=latest
