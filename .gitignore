# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
.env.local

# Node
node_modules/
dist/
build/
.vite/
*.log

# IDE
.vscode/
.idea/
.DS_Store

# Windows
Thumbs.db
[Dd]esktop.ini
$RECYCLE.BIN/
*.lnk

# Local
*.local

# App data + secrets — NEVER commit
backend/storage/
backend/browser_profiles/
browser_profiles/
backend/.env
frontend/.env
.env
.env.prod
.env.staging
*.pem
*.key
cookies.txt

# Claude tooling — keep settings/transcripts/local out of git, but commit
# the project-scoped skill catalog so every contributor gets the same set.
.claude/*
!.claude/skills/
!.claude/skills/**

# Deploy probe scripts (one-shot debugging)
deploy/probe*.py
deploy/inspect*.py

# Build artefacts
*.tgz
frontend/dist/

check_domain.py
deploy_fe.py

# Backup credentials — DO NOT commit
.backup-env

