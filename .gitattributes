# Force LF on files that ship into Linux containers.
# Without this, Windows clones convert them to CRLF and Docker complains:
#   env: 'bash\r': No such file or directory
*.sh        text eol=lf
Dockerfile* text eol=lf
*.yml       text eol=lf
*.yaml      text eol=lf
*.py        text eol=lf
